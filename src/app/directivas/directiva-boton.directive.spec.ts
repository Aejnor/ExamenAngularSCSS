import { DirectivaBotonDirective } from './directiva-boton.directive';

describe('DirectivaBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaBotonDirective();
    expect(directive).toBeTruthy();
  });
});
